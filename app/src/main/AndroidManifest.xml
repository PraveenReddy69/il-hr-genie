<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- HR Genie answers from the policy knowledge base over HTTPS. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Hold-to-talk dictation in the chat composer. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Android 13+ gates notifications behind a runtime grant. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        Android 11+ package visibility: without these, queryIntentActivities and
        resolveActivity return nothing, so the cropper finds no gallery or camera to
        offer and the mail intent looks unhandled. No permissions are needed — only
        the declaration that we look for these apps.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PICK" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <!--
            Without this, SpeechRecognizer.isRecognitionAvailable() returns false on
            Android 11+ even when a recogniser is installed: the service is simply
            invisible to us.
        -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".HrGenieApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_main"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_main_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ILHRGenie">
        <!-- Ticket updates pushed by the backend. Data-only messages, so this is
             called whether the app is foreground, backgrounded or dead. -->
        <service
            android:name=".push.HrGenieMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/blue_primary" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="hr_genie_tickets" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Give the cropper an action bar so its Done item has somewhere to show. -->
        <activity
            android:name="com.canhub.cropper.CropImageActivity"
            android:theme="@style/HG.Cropper"
            tools:replace="android:theme" />
    </application>

</manifest>