<?xml version="1.0" encoding="utf-8"?>
<!-- Unread marker on the chat button. Ringed in the app background so it reads as a
     badge sitting on top rather than a dot painted onto the button. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/red_badge" />
    <stroke
        android:width="2.5dp"
        android:color="@color/bg_app" />
</shape>
