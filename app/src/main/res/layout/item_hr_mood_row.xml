<?xml version="1.0" encoding="utf-8"?>
<!-- One mood band: emoji + label, the head count, and a share-of-responses bar. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="6dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/moodLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="sans-serif-medium"
            android:textColor="@color/ink"
            android:textSize="@dimen/text_body_sm"
            tools:text="🙂 Good" />

        <TextView
            android:id="@+id/moodCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_slate"
            android:textSize="@dimen/text_caption_lg"
            android:textStyle="bold"
            tools:text="2" />
    </LinearLayout>

    <FrameLayout
        android:id="@+id/moodTrack"
        android:layout_width="match_parent"
        android:layout_height="@dimen/track_height"
        android:background="@drawable/bg_track">

        <View
            android:id="@+id/moodTrackFill"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:background="@drawable/bg_track_fill_blue" />
    </FrameLayout>
</LinearLayout>
